import React from 'react';

import {DefaultText} from '../../components/DefaultText';

import {
    Container
} from "./styled";

export default () => {
    return(
        <Container>
            <DefaultText>Olá mundo</DefaultText>
        </Container>
    );
}